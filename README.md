# -ts-node-mongo-rest-api
REST API using MongoDB, Node.js and TypeScript



### Install packages

```shell
npm i
```

### Setup MongoDB URL

In `src/index.ts`:

```js
const MONGO_URL = ''; // DB URI
```

### Start the app

```shell
npm start
```
